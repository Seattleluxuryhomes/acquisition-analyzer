# Rename this file to exactly  .env  (no .txt). Keys live here only — never in code.

# --- Acquisition Analyzer ---
ATTOM_API_KEY=paste_your_attom_key_here
ANTHROPIC_API_KEY=paste_your_claude_console_key_here
PORT=3000

# --- Showcase Video Maker (Arcads) ---
# Get this from https://app.arcads.ai/settings/api  (or run ./scripts/setup.sh).
# Use EITHER the full Basic header (preferred) ...
ARCADS_BASIC_AUTH=Basic paste_your_base64_credentials_here
# ... OR the raw key (key as username, empty password):
# ARCADS_API_KEY=paste_your_arcads_key_here
# Optional: pin a specific Arcads product (else the first product is used):
# ARCADS_PRODUCT_ID=

# --- Optional: auto-fetch Zillow price + photos from a URL ---
# Without this you upload listing photos by hand (Zillow blocks scraping).
# A RapidAPI Zillow provider key (e.g. zillow-com1):
# RAPIDAPI_KEY=paste_your_rapidapi_key_here
# RAPIDAPI_ZILLOW_HOST=zillow-com1.p.rapidapi.com
